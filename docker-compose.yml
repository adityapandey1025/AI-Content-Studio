services:
  ai-content-studio:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: ai-content-studio
    ports:
      - "3000:3000"
    env_file:
      - .env
    restart: unless-stopped

